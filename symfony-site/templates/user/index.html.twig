{% extends 'layout.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block content %}

    <style>
        .users-index{
            background: var(--vio2);
            padding: 5rem 0;
        }

        .user-img{
            width: 8rem;
            height: 8rem;
            border-radius: 0.8rem 0 0 0.8rem;
            background: var(--bg1);
            overflow: hidden;
            border: 2px solid #fff;
        }

        .user-img img{
            width: 100%;
        }

        .user-content{
            position: relative;
        }

        .user-lastname, .user-destination, .user-comment{
            padding: 0.2rem 1rem;
            margin: 0 0 0.9rem 0;
            background: var(--bg2);
            display: inline-block;
            border-radius: 0 1rem 1rem 0;
        }

        p.user-destination, p.user-comment{
            display: block;
        }

        .user-comment{
            margin: 0;
            position: absolute;
            bottom: 0;
        }
    </style>

    <div class="container">
        <div class="row">
            {% for user in users %}
            <div className="col-6 col-md-4 mt-4">
                <div className="user-container d-flex justify-content-start">
                    <div className="user-img">
                        <img src={profil01} alt="image du profil"></img>
                    </div>
                    <div className="user-content">
                        <h5 className="user-lastname">{{ user.prenom }}</h5>
                        <p className="user-destination"><span>Date : </span>{{ user.dateDispo|date('d/m/Y') }}</p>
                        <p className="user-comment">bla bla</p>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
