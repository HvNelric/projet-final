{% extends 'layout.html.twig' %}

{% block content %}

    <div class="container">
        <div class="row mt-5">
            {% for region in regions.sejourRegion %}
                <ul>
                    <li>
                        {{ region.ville }}
                    </li>
                </ul>
            {% endfor %}
            {{ dump(regions) }}
        </div>
    </div>

{% endblock %}
